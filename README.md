to-do

1. intro.js Add intro cards for easy filling. FTC flag webstorage persistance gated and add the same text to a ? popover for later reference
2. under the rules transfer text to popoever
3. investigate if the app can be optimized for faster and snappy experience. Use dev tools' profiler (from mcp) to measure and improve it.
4. BE for login and storing in BE. Add friends with code or companyconfiguration code. Design the system to be scalable
